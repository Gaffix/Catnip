# Repository instructions

- Whenever the user asks to push changes, also build the mod and publish a GitHub release using the current project version.
- Attach the compiled release JAR (not the sources JAR) to the GitHub release.
